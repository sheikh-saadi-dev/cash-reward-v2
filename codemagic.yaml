workflows:
  android-workflow:
    name: Android Build
    instance_type: mac_mini_m2
    max_build_duration: 60

    environment:
      java: 17

    scripts:
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
